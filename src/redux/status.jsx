import { createSlice } from "@reduxjs/toolkit"
const initialState = {

}

 export const appMcsSlice = createSlice({
    name: "appMcs",
    initialState,
    reducers: {
   
    }

})

export const { } = appMcsSlice.actions
export default  appMcsSlice.reducer